# =============================================================================
# ST Open Source - Environment Configuration
# =============================================================================

# Server Configuration
NODE_ENV=development
PORT=3000
VERSION=0.1.0

# Docker Configuration (set automatically during build)
# DOCKER_TAG=latest

# =============================================================================
# ST Open Source - Environment Configuration
# =============================================================================

# Server Configuration
NODE_ENV=development
PORT=3000
VERSION=0.1.0

# Docker Configuration (set automatically during build)
# DOCKER_TAG=latest

# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/st_open_source"

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters
JWT_EXPIRES_IN=7d

# Rate Limiting
RATE_LIMIT_POINTS=500
RATE_LIMIT_DURATION=120

# CORS
CORS_ORIGIN=http://localhost:3000

# Request Limits
MAX_REQUEST_SIZE=10mb

# Logging
LOG_LEVEL=info

# Loki Logging (Optional)
# LOKI_HOST=http://localhost:3100
# LOKI_APP_NAME=st-open-source

# =============================================================================
# Development Notes
# =============================================================================
# 
# 1. Copy this file to .env and update the values
# 2. DATABASE_URL: Use your PostgreSQL connection string
# 3. JWT_SECRET: Generate a secure random string (min 32 chars)
# 4. LOKI_HOST: Only needed if using Grafana Loki for logging
# 5. DOCKER_TAG: Set automatically during Docker build with --build-arg
# 
# ============================================================================= 